@page
@model BlogApp.Pages.Profile.EditModel
@inject LocalManager LM
@{
    ViewBag.Title = "Profile Edit";
    Layout = "_HomeLayout";
    Language lang = Html.ViewContext.HttpContext.GetCurrentLanguage();
}

<partial name="_JqueryValidation" />
<div class="m-2 bg-warning bg-opacity-25 border border-4 border-warning">
    <h1 class="m-2 bg-warning text-black text-center">@LM.GetLocalOrKey("Edit", lang) @LM.GetLocalOrKey("Profile", lang)</h1>
    <div class="m-2">
        <form asp-page="Edit" method="post" enctype="multipart/form-data">
            <div class="form-control m-2">
                <div class="m-2 text-danger" asp-validation-summary="ModelOnly"></div>
            </div>
            <input hidden asp-for="@Model.UserId" />
            <div class="form-control m-2">
                <label class="m-1">@LM.GetLocalOrKey("UserName", lang)</label>
                <span class="text-danger" asp-validation-for="@Model.UserName"></span>
                <input class="input-group-text form-text m-2" asp-for="@Model.UserName" />
            </div>
            <div class="form-control m-2">
                <label class="m-1">@LM.GetLocalOrKey("Email", lang)</label>
                <span class="text-danger" asp-validation-for="@Model.Email"></span>
                <input class="input-group-text form-text m-2" asp-for="@Model.Email" />
            </div>
            <div class="form-control m-2">
                <label class="m-1">@LM.GetLocalOrKey("Description", lang)</label>
                <span class="text-danger" asp-validation-for="@Model.Description"></span>
                <textarea class="form-control-plaintext input-group-text text-start" asp-for="@Model.Description"></textarea>
            </div>
            <div class="form-control m-2">
                <label class="m-1">@LM.GetLocalOrKey("Profile Picture", lang)</label>
                <img class="m-2" src="@(Url.Content(Model.ProfilePictureUrl))" alt="User Icon" height="64" width="64" />
                <div class="m-2">
                    <label class="m-2 text-info">@LM.GetLocalOrKey("Should change profile picture", lang)?</label>
                    <input asp-for="@Model.ShouldChangeProfilePicture" id="changepic" />
                </div>             
                <div class="m-2" id="div1">                  
                    <div class="m-2" id="div2">
                        <span class="text-danger" asp-validation-for="@Model.ProfilePicture"></span>
                        <input type="file" class="input-group-text form-text m-2" asp-for="@Model.ProfilePicture" />
                    </div>
                    <div class="m-2 bg-black bg-opacity-10">
                        <label class="m-2 text-danger">@LM.GetLocalOrKey("Delete current picture", lang)?</label>
                        <input asp-for="@Model.ShouldDeleteProfilePicture" id="deletepic" />
                    </div>
                </div>               
            </div>
            <button class="m-2 btn btn-outline-success">@LM.GetLocalOrKey("Save", lang)</button>
        </form>
    </div>
</div>

<script>
    // implement colapse function on ShouldChangeProfilePicture check box:
    let changePic = @(Model.ShouldChangeProfilePicture.ToString().ToLower())
    let deletePic = @(Model.ShouldDeleteProfilePicture.ToString().ToLower())

    window.addEventListener("DOMContentLoaded",()=>
    {
        let changeCheckBox = document.getElementById("changepic");
        let deleteCheckBox = document.getElementById("deletepic");
        let div1 =  document.getElementById("div1");
        let div2 =  document.getElementById("div2");
        changeCheckBox.addEventListener("click",()=>
        {
            changePic = !changePic;
            div1.hidden = !changePic;
        });
        deleteCheckBox.addEventListener("click", () =>
        {
            deletePic = !deletePic;
            div2.hidden = deletePic;
        });

        // set hidden for the first time:
        div1.hidden = !changePic;
        div2.hidden = deletePic;
    })
</script>
